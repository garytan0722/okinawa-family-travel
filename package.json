{
  "name": "okinawa-family-travel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "serve": "python3 -m http.server 4173",
    "check": "npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}

